"""Other libraries' interfaces, answered by sluicer's own readers.

``from sluicer.compat import extruct`` in place of ``import extruct`` is the
whole migration for a caller of extruct: the same functions, the same
arguments, the same shape of answer. Where the answer differs it is because
extruct's is wrong, and ``docs/extruct.md`` lists each difference and says why.
"""
