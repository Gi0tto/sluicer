"""Hold what a page declares to what the documentation asks of it.

The rules come first, as data: what Google's structured-data documentation
asks of each rich-result feature (``google``), the part of schema.org's type
tree and enumerations those rules need (``schema_org``), and the checks one
declared value can fail (``values``). Every rule names the page it comes from.
"""
